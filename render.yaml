services:
  - type: web
    name: Brand-Tone-Enforcer
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr libtesseract-dev
      pip install -r requirements.txt
    startCommand: python main.py
